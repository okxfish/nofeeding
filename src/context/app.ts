import React from "react";

export const CurrenActivedFeedIdContext = React.createContext<any>(null);
export const DispatchContext = React.createContext<any>(null);
export const StoreContext = React.createContext<any>(null);